// AUTO-GENERATED — DO NOT EDIT
// Generated by forge generate
//
// Deterministic mock data for {{ .EntityName }}. Mirrors the values
// produced by db/fixtures so the mock transport and the seeded test
// database return identical objects for the same identifiers.

import { create } from "@bufbuild/protobuf";
import { timestampFromDate } from "@bufbuild/protobuf/wkt";
import { {{ .SchemaImport }} } from "@/gen/{{ .ImportPath }}";

void timestampFromDate;

export const {{ .EntitySlug | camelCase }} = [
{{- range .Records }}
  create({{ $.SchemaImport }}, {
{{- range .Fields }}
    {{ .Name }}: {{ .Value }},
{{- end }}
  }),
{{- end }}
];
