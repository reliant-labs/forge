// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge project disown to take ownership)
//
// Deterministic mock data for {{ .EntityName }}. Every value below is the
// one the project's own seed plan writes into that cell, so the mock
// transport and a `forge db seed apply` database agree row for row.
// Domain vocabulary comes from db/seeds/vocab.yaml; a column nothing
// describes carries the same synthetic placeholder in both places.

import { create } from "@bufbuild/protobuf";
import { timestampFromDate } from "@bufbuild/protobuf/wkt";

import { {{ .SchemaImport }} } from "@/gen/{{ .ImportPath }}";

void timestampFromDate;

export const {{ .EntityNamePlural | camelCase }} = [
{{- range .Records }}
  create({{ $.SchemaImport }}, {
{{- range .Fields }}
    {{ .Name }}: {{ .Value }},
{{- end }}
  }),
{{- end }}
];
