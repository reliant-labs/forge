// yours: scaffolded once, never touched again — forge will not overwrite this file
//
// Default scenario — the "nothing overridden" fallback.
//
// This file is seeded once by `forge generate` and is safe to edit,
// but the rule is: keep it empty. New named scenarios are additive
// overlays; if a request doesn't match a scenario handler it falls
// through to the base fixture transport. There is nothing to override
// here.
//
// To add a new scenario, run:
//   forge add scenario <name>

import { defineScenario } from "../scenario-types";

export default defineScenario({
  name: "default",
  description: "Empty fallback — base fixtures only",
  handlers: {},
});
