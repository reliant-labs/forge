// AUTO-GENERATED — DO NOT EDIT
// Generated by forge generate
//
// Scenario registry. Re-exports every scenario file under
// src/mocks/scenarios/ and exposes a `byName(name)` lookup keyed on the
// `name` field declared inside each scenario. Regenerated each time
// `forge generate` runs.

{{- range .Scenarios }}
import {{ .Ident }} from "./{{ .Slug }}";
{{- end }}

import type { Scenario } from "../scenario-types";

const registry: Record<string, Scenario> = {
{{- range .Scenarios }}
  [{{ .Ident }}.name]: {{ .Ident }},
{{- end }}
};

/**
 * byName resolves a scenario by its declared `name` field — the value
 * passed in the `?scenario=` URL param. Returns `undefined` when no
 * scenario matches.
 */
export function byName(name: string): Scenario | undefined {
  return registry[name];
}

/** All scenarios in the registry. Useful for the dev overlay listing. */
export function all(): Scenario[] {
  return Object.values(registry);
}

// Stable named re-export of the empty fallback. Mock transports import
// this so navigating without `?scenario=` (or with an unknown value)
// still has a Scenario object to call `.setup?.()` and dispatch through.
export { default as defaultScenario } from "./default";
