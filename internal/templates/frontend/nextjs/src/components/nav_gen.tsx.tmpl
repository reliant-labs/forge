// Code generated by forge generate. DO NOT EDIT.
// Source: forge.yaml services + frontend pages
//
// This file holds the route table for the sidebar nav and dashboard. It is
// regenerated every `forge generate` from the project's services/entities.
//
// Hand-edit `nav.tsx` (the sibling Tier-2 file) to curate which routes show
// up in the sidebar, reorder them, attach icons, etc. Editing this file
// will trip the `.forge/checksums.json` Tier-1 guard and abort the next
// `forge generate` (override with `--force` or `--accept`).

/**
 * RouteSpec is the typed shape exported alongside ALL_ROUTES so the
 * user-curated nav.tsx (and dashboard tiles in app/page.tsx) can compose
 * routes with full type safety.
 */
export type RouteSpec = {
  /** URL path, always rooted (e.g. "/billing"). */
  path: string;
  /** Display label, capitalized for menus and tile headers. */
  label: string;
  /** Lowercase variant, useful for descriptions ("manage billing"). */
  labelLower: string;
  /** Slug — the path segment without the leading slash. */
  slug: string;
};

/**
 * ALL_ROUTES enumerates every CRUD entity surfaced by the project's
 * services. The user-owned nav.tsx imports this and decides which subset
 * to render; the dashboard imports it to build entity tiles.
 */
export const ALL_ROUTES: RouteSpec[] = [
{{- range .Pages}}
  { path: "/{{.Slug}}", label: "{{.Label}}", labelLower: "{{.LabelLower}}", slug: "{{.Slug}}" },
{{- end}}
];
