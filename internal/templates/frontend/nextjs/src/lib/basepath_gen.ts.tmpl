// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge project disown to take ownership)
// Source: forge.yaml frontends[].base_path
//
// The URL path prefix this frontend is mounted under (e.g. "/admin" behind a
// reverse proxy that blends several apps on one host). "" means the host root.
//
// This file is the VALUE only. The behaviour — normalising the prefix,
// prefixing a path idempotently, passing absolute URLs through — lives in
// @reliant-labs/web-runtime, where it is written and tested once instead of
// being re-emitted into every project. Only the two things that are genuinely
// this project's are here: forge.yaml's base_path, and the env read.
//
// THE ENV READ CANNOT MOVE INTO THE LIBRARY. Next.js inlines
// NEXT_PUBLIC_* by substituting the literal into the sources it compiles, so
// a library reading process.env at runtime finds nothing in a browser bundle.
// The literal has to appear verbatim in project code — here.
//
// WHEN YOU DO NOT NEED THIS FILE:
//   - `<Link href="/tasks">` and `useRouter().push("/tasks")` — Next.js
//     prepends the configured basePath to both automatically. Keep passing
//     app-relative paths there; joinBasePath() would be a no-op wrapped in
//     noise (and it is idempotent, so an accidental double-wrap is harmless).
//
// WHEN YOU DO — the URLs Next.js never sees:
//   - window.location-built absolute URLs handed to external systems that
//     round-trip back into the app: payment-provider redirect / return URLs
//     (Stripe success_url / cancel_url), OAuth redirect_uri, share links,
//     magic-link emails:
//       const successUrl =
//         window.location.origin + joinBasePath("/billing/success");
//   - raw `fetch()` / `EventSource` / `<a>` / `<img>` paths built by string
//     concatenation rather than rendered through Next's router.
//
// The anti-pattern this kills: hand-baked prefix literals (`"/admin" + path`)
// and bare `/route` strings in hand-built URLs — they bypass the prefix or
// double it the day the mount point changes.
//
// NEXT_PUBLIC_BASE_PATH is the ONLY base-path env var forge reads or writes,
// and the same name next.config.ts uses for `basePath` / `assetPrefix`.
import { createBasePath } from "@reliant-labs/web-runtime";

export const { BASE_PATH, joinBasePath } = createBasePath(
  process.env.NEXT_PUBLIC_BASE_PATH ?? "{{.BasePath}}",
);
