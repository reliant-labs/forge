// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge project disown to take ownership)
//
// Browser OpenTelemetry CONFIGURATION. The SDK wiring it configures — the
// WebTracerProvider, the batched OTLP/HTTP exporter, W3C propagation and the
// fetch / XHR / document-load auto-instrumentations — lives in
// @reliantlabs/forge-web-runtime/otel.
//
// What stays here is the part that CANNOT move: the `process.env.NEXT_PUBLIC_*`
// reads. Next.js inlines those literals into the bundle at BUILD time, and it
// can only do that where they are written out verbatim in application code. A
// library reading `process.env` at runtime finds nothing in a browser, so
// hoisting these two lines into the package would silently disable tracing
// for every project.
//
// The engine arrives through the "/otel" SUBPATH, never the barrel: it pulls
// the eight @opentelemetry/* SDK packages, which only the Next.js scaffold
// installs (a Vite SPA or React Native frontend declares @opentelemetry/api
// alone).
//
// This is the EXPORT path, and it is opt-in: nothing ships anywhere without
// NEXT_PUBLIC_OTEL_ENDPOINT. Distributed tracing itself — the traceparent
// attached to every RPC, which is what makes browser spans join the backend's
// trace — is always on, needs no collector, and does NOT depend on this
// module.
import { initBrowserTracing } from "@reliantlabs/forge-web-runtime/otel";

/**
 * initTelemetry starts browser trace export. Call it once from the app
 * shell (providers.tsx already does).
 *
 * A no-op without NEXT_PUBLIC_OTEL_ENDPOINT (nothing to export to) and
 * without a DOM (SSR / prerender), and idempotent under React StrictMode's
 * double-mount and Fast Refresh — so it is safe to call unconditionally.
 */
export function initTelemetry(serviceName = '{{.ProjectName}}-frontend') {
  initBrowserTracing({
    endpoint: process.env.NEXT_PUBLIC_OTEL_ENDPOINT || '',
    serviceName,
    serviceVersion: process.env.NEXT_PUBLIC_APP_VERSION || '0.0.0',
  });
}

export { context, getTracer, propagation, trace } from '@reliantlabs/forge-web-runtime/otel';
