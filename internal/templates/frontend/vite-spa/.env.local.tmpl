VITE_API_URL=http://localhost:{{.ApiPort}}

# Mock mode is OPT-IN. The default is the real backend (start it with
# `forge run`). Set to "true" for offline UI-only development — a
# visible banner will remind you the backend is NOT being contacted —
# or "hybrid" to overlay ?scenario= mocks on the real backend.
# VITE_MOCK_API=true
