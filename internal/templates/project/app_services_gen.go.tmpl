// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge disown to take ownership)
// Source: proto/services + internal/{handlers,workers,operators}/<x>/contract.go.
//
// Services is the typed registry-of-instances the generated Build
// (inject_gen.go) returns and PostBuild (post_build.go) wires. Each field
// is the component's Service INTERFACE (not a pointer to it) — so a field
// is agnostic to whether it holds the real in-process service, a Connect
// client, or a mock (FORGE_SHAPE_REDESIGN §2). Adding/removing a component
// regenerates this struct.
package app

import (
{{- range .Components}}
	{{.Alias}} "{{$.Module}}/{{.ImportPath}}"
{{- end}}
)

// Services holds every constructed component instance for one binary, each
// typed as its Service interface.
type Services struct {
{{- range .Components}}
	{{.FieldName}} {{.FieldType}}
{{- end}}
}
