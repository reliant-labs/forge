// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge disown to take ownership)
// Source: proto/services — the per-service subcommand registration roster.
//
// addServiceCmds registers one REAL cobra subcommand per service (each
// defined in its own svc_<name>.go file). root.go calls this. A service
// whose kebab name collides with a built-in (server / version / db / help /
// completion) is SKIPPED here (a NOTE is emitted below) and remains
// reachable via `server <name>` (the typed MountByName convenience).
package cli

import (
	"github.com/spf13/cobra"
)

// addServiceCmds wires every per-service subcommand onto root.
func addServiceCmds(root *cobra.Command, deps Deps) {
{{- range .Services}}
	root.AddCommand(new{{.FieldName}}Cmd(deps))
{{- end}}
{{- if not .Services}}
	_, _ = root, deps
{{- end}}
}
{{- range .Skipped}}

// NOTE: service "{{.}}" collides with a built-in subcommand name
// (server / version / db / help / completion), so no per-service subcommand
// was generated for it. Run it via `server {{.}}`.
{{- end}}
