// Code generated by forge. DO NOT EDIT.
// Source: forge.yaml (kind: cli).
//
// To customize: hand-written subcommands belong in their own files in
// this package. This main.go entrypoint is regenerated on every
// `forge generate`.
package main

import (
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

// rootCmd is the top-level Cobra command for the {{.Name}} CLI.
//
// Add new subcommands by declaring them in their own files in this
// package and calling rootCmd.AddCommand from an init() in that file
// — see version.go for the pattern.
var rootCmd = &cobra.Command{
	Use:   "{{.Name}}",
	Short: "{{.Name}} command-line tool",
	Long:  "{{.Name}} — TODO: replace this description with what your CLI does.",
}

// Execute runs the root command and exits the process with a non-zero
// status on error. Kept as a separate function so tests can drive the
// command tree without going through main().
func Execute() {
	if err := rootCmd.Execute(); err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
}

func main() {
	Execute()
}
