// Code generated by forge. DO NOT EDIT.
// Source: forge.yaml (kind: cli).
//
// To customize: replace the cli flag set in main.go. This version
// subcommand is regenerated on every `forge generate`.
package main

import (
	"fmt"
	"runtime/debug"

	"github.com/spf13/cobra"
)

// version, commit, and date are stamped at build time via -ldflags so the
// CLI can report exactly what's running:
//
//   go build -ldflags="-X main.version=v1.0.0 -X main.commit=abc123 -X main.date=2026-01-01" ./cmd/{{.Name}}
//
// `dev` / `none` / `unknown` are visible defaults so an unstamped build
// announces itself rather than impersonating a release.
var (
	version = "dev"
	commit  = "none"
	date    = "unknown"
)

var versionCmd = &cobra.Command{
	Use:   "version",
	Short: "Print version information",
	Run: func(cmd *cobra.Command, args []string) {
		fmt.Printf("{{.Name}} %s\n", version)
		fmt.Printf("  commit: %s\n", commit)
		fmt.Printf("  built:  %s\n", date)
		if info, ok := debug.ReadBuildInfo(); ok {
			fmt.Printf("  go:     %s\n", info.GoVersion)
		}
	},
}

func init() {
	rootCmd.AddCommand(versionCmd)
}
