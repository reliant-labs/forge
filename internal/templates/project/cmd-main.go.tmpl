// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge disown to take ownership)
// Source: internal/templates/project/cmd-main.go.tmpl.
//
// Thin entrypoint (devspace idiom): all command logic lives under
// cmd/{{.Name}}/cmd (package cmd — root.go assembles the tree) and its
// command-group SUBPACKAGES (services/, workers/, operators/). This main is
// a thin cmd.Execute(); the blank imports below run each group's init() so
// its commands self-register onto the root.
//
// binary=shared only changes the DEPLOY story (one image serving many
// services via cobra subcommands); the command tree itself is identical to
// per-service mode. To run a specific service: `{{.Name}} <service>`. To
// list services: `{{.Name}} --help`.
package main

import (
	"{{.Module}}/cmd/{{.Name}}/cmd"

	// Group subpackages self-register their commands via init(). Blank-
	// imported here (not in package cmd) so cmd → groups → cmd is not a
	// cycle: groups import cmd for the shared Deps/Serve helpers, and the
	// root tree is populated from the registry cmd owns.
	_ "{{.Module}}/cmd/{{.Name}}/cmd/operators"
	_ "{{.Module}}/cmd/{{.Name}}/cmd/services"
	_ "{{.Module}}/cmd/{{.Name}}/cmd/workers"
)

func main() {
	cmd.Execute()
}
