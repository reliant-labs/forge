// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge disown to take ownership)
// Source: forge.yaml `binary: shared` mode.
//
// Thin entrypoint (gh / cobra-cli idiom). binary=shared only changes the
// deploy story (one image serving many services via cobra subcommands); the
// command tree itself lives in internal/cli (root.go + one file per
// command), so this main is a thin cli.Execute() exactly like per-service
// mode. To run a specific service: `{{.Name}} <service>`. To list services:
// `{{.Name}} --help`.
package main

import (
	"{{.Module}}/internal/cli"
)

func main() {
	cli.Execute()
}
