package main

// Code generated by forge. DO NOT EDIT.
// Source: forge.yaml `binary: shared` mode.
//
// In binary=shared projects, each service is emitted as its own cobra
// subcommand under cmd/{{.Name}}/<svc>.go. Calling `./{{.Name}} <svc>`
// runs that one service; `./{{.Name}} --help` lists them all. The
// canonical `./{{.Name}} server [services...]` entry point still works
// (kept for back-compat with existing tooling) and routes through the
// same `app.BootstrapOnly` codepath.

import (
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

var rootCmd = &cobra.Command{
	Use:   "{{.Name}}",
	Short: "{{.Name}} — shared-binary entrypoint (one image, many cobra subcommands)",
	Long: `{{.Name}} ships as a single Go binary that exposes one cobra
subcommand per service. This is the binary=shared codegen mode (see
forge.yaml). To run a specific service:

    {{.Name}} <service>

To list all available services:

    {{.Name}} --help

Each subcommand is a thin shim over pkg/app.BootstrapService(name) that
constructs only the named service's dependency graph and serves it.`,
}

// Execute is the program entry point.
func Execute() {
	if err := rootCmd.Execute(); err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
}

func main() {
	Execute()
}
