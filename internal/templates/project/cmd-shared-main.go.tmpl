// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge disown to take ownership)
// Source: forge.yaml `binary: shared` mode.
//
// Subcommand surface (same as the per-service binary mode — shared mode
// only changes the deploy story, one image serving many services):
//   - cmd/server.go      — `{{.Name}} server [services...]` (generated)
//   - cmd/services_gen.go — one subcommand per RegisteredServices row
//     (generated; the cobra projection of pkg/app/services.go)
//   - cmd/commands.go    — userCommands(), YOUR extension point for
//     second binaries / custom tooling (user-owned, scaffolded once)

package main

import (
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

var rootCmd = &cobra.Command{
	Use:   "{{.Name}}",
	Short: "{{.Name}} — shared-binary entrypoint (one image, many cobra subcommands)",
	Long: `{{.Name}} ships as a single Go binary that exposes one cobra
subcommand per registered service (see pkg/app/services.go — what this
binary serves is code, not config). This is the binary=shared mode
(forge.yaml). To run a specific service:

    {{.Name}} <service>

To list all available services:

    {{.Name}} --help

Each service subcommand runs the canonical server pipeline with the
service-name filter pre-filled, so only that service's dependency
graph is mounted.`,
}

// Execute is the program entry point.
func Execute() {
	if err := rootCmd.Execute(); err != nil {
		fmt.Fprintln(os.Stderr, err)
		os.Exit(1)
	}
}

func main() {
	// userCommands (cmd/commands.go, user-owned): second binaries and
	// custom tooling register as code — same posture as the service
	// registration rows in pkg/app/services.go.
	for _, c := range userCommands() {
		rootCmd.AddCommand(c)
	}
	Execute()
}
