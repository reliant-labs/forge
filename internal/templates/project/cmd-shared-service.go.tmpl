// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge disown to take ownership)
// Source: forge.yaml `binary: shared` mode + service "{{.ServiceName}}".
//
// Thin cobra subcommand that runs ONLY the {{.ServiceName}} service.
// Constructs the {{.ServiceName}}-only dependency graph via
// app.BootstrapService("{{.ServiceName}}", ...) and serves its
// handlers on the configured port. Other services' constructors are
// not invoked from this subcommand.

package main

import (
	"github.com/spf13/cobra"
)

var {{.ServicePackage}}Cmd = &cobra.Command{
	Use:   "{{.ServiceName}}",
	Short: "Run the {{.ServiceName}} service",
	Long:  "Run the {{.ServiceName}} service. Constructs only this service's dependency graph and mounts only its handlers on the HTTP mux.",
	RunE: func(cmd *cobra.Command, args []string) error {
		// Reuse the canonical server pipeline (config load, OTel setup,
		// listener, graceful shutdown) and pre-fill the service-name filter
		// so only {{.ServiceName}} is constructed and registered.
		return runServer(cmd, []string{"{{.ServiceName}}"})
	},
}

func init() {
	{{.ServicePackage}}Cmd.Flags().AddFlagSet(serverCmd.Flags())
	rootCmd.AddCommand({{.ServicePackage}}Cmd)
}
