// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge project disown to take ownership)
// Source: proto service {{.Name}} — the TYPED mount method for this service.
//
// THE ONE DERIVED FACT of the {{.Name}} subcommand: which method on
// *app.Components mounts it.
//
// The name is not a spelling forge can guess and leave alone — it is resolved
// against the whole project. A service whose handler package collides
// cross-role with an internal package (handler internal/handlers/billing
// `package billing` vs domain internal/billing `package billing`) mounts as
// MountSvcBilling rather than MountBilling, and that resolution can CHANGE when
// an unrelated package is added. So it is re-derived on every run, from the
// same single source that emits the Components method itself — which is what
// guarantees this expression always names a method that exists.
//
// It is a var of MountFunc rather than a call so the reference is a
// compile-time typed method expression: rename the service and this file
// stops compiling instead of failing at runtime with an unmounted route.
//
// {{.Name}}.go next door is YOURS — the flags, the RunE, the help text.
// Keeping the two apart means adding a service never rewrites your command.
package services

import (
	"{{.Module}}/internal/app"
)

// mount{{.FieldName}} is the typed mount method expression for the {{.Name}}
// service — (*app.Components).Mount{{.MountFieldName}}.
var mount{{.FieldName}} app.MountFunc = (*app.Components).Mount{{.MountFieldName}}
