// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge disown to take ownership)
// Source: proto/services — the per-service command-group anchor.
//
// Each service's subcommand lives in its own <name>.go in this package and
// self-registers via init() (cmd.RegisterServiceCmd). This file is the group
// ANCHOR: it guarantees the package compiles even with zero services (so
// main.go's blank import resolves) and records the collision NOTEs for any
// service whose kebab name shadows a built-in (server / version / db / help /
// completion) — those are skipped here and remain reachable via `server <name>`.
package services
{{- range .Skipped}}

// NOTE: service "{{.}}" collides with a built-in subcommand name
// (server / version / db / help / completion), so no per-service subcommand
// was generated for it. Run it via `server {{.}}`.
{{- end}}
