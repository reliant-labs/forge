// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge project disown to take ownership)
// Source: internal/templates/project/cmd-tree-db-source.go.tmpl (driven by db/migrations/*.sql).
//
// THE MIGRATION SOURCE — the one part of the `db migrate` command that forge
// re-derives, and the reason it is a separate file from db.go.
//
// db/embed.go (package db, `forgedb.MigrationsFS`) is generated ONLY when
// db/migrations/ holds at least one .sql file. So whether the symbol this
// file returns EXISTS is a fact about what you have written, re-read from
// disk on every run — which is exactly what Tier-1 is for. db.go next door
// holds the migration POLICY and is yours; keeping the two apart means your
// policy edits never collide with this re-derivation, and adding your first
// migration does not require forge to rewrite a file you own.
package cmd

import (
	"io/fs"
{{- if .HasMigrations}}

	forgedb "{{.Module}}/db"
{{- end}}
)

// migrationSourceDir is the directory INSIDE the embedded filesystem
// (db/embed.go's `//go:embed migrations/*.sql`) that holds the SQL.
const migrationSourceDir = "migrations"

{{if .HasMigrations -}}
// migrationSource returns the migration set compiled into this binary.
func migrationSource() fs.FS { return forgedb.MigrationsFS }
{{- else -}}
// migrationSource returns nil: db/migrations/ held no .sql file when this
// was generated, so db/embed.go (forgedb.MigrationsFS) does not exist and
// referencing it would not compile.
//
// nil is not a silent no-op — migratekit.Open REFUSES a nil source with an
// error naming the missing embed, so `db migrate up` fails loudly instead of
// exiting 0 against an unmigrated database. Write a migration (`forge db
// migration new <name>`) and re-run `forge generate` to embed it.
func migrationSource() fs.FS { return nil }
{{- end}}
