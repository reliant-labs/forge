// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge disown to take ownership)
// Source: internal/templates/project/cmd-tree-server.go.tmpl.
//
// The all-services command. `{{.Name}} server` mounts EVERY service via the
// typed (*app.Components).MountAll method expression and runs every worker/
// operator. There is NO `server <subset>` string-selected path: to run a single
// service, use its dedicated typed subcommand (cmd/services/<name>.go), which
// names the (*app.Components).Mount<Svc> method DIRECTLY — there is no
// name->func lookup table anywhere in the run path.
package cmd

import (
	"github.com/spf13/cobra"

	"{{.Module}}/internal/app"
	"{{.Module}}/pkg/config"
)

func newServerCmd(deps Deps) *cobra.Command {
	cmd := &cobra.Command{
		Use:   "server",
		Args:  cobra.NoArgs,
		Short: "Start the HTTP/Connect server (all services)",
		Long:  "Start the HTTP/Connect server: every service is mounted and all workers/operators run. To run a single service, use its typed subcommand.",
		RunE: func(cmd *cobra.Command, args []string) error {
			deps.Cmd = cmd
			// The all-services process composes ITSELF: the typed MountAll method
			// expression, AllWorkers + AllOperators naming every supervised
			// component directly, and RequireComplete turning on the boot-time
			// mount-completeness gate.
			return Serve(cmd.Context(), deps, ServeSpec{
				Mount:           (*app.Components).MountAll,
				Workers:         (*app.Components).AllWorkers,
				Operators:       (*app.Components).AllOperators,
				RequireComplete: true,
			})
		},
	}
	config.RegisterFlags(cmd)
	return cmd
}
