// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge disown to take ownership)
// Source: proto/services + each service's Deps struct.
//
// TWO surfaces, ONE source (FORGE_SHAPE_REDESIGN §1/§2):
//
//   - TYPED MOUNTS — the run path. Each service has a typed method
//     `(*Services).Mount<Svc>(mux, opts...) string` that mounts EXACTLY
//     that service over the constructed *Services and returns its Connect
//     path. `MountAll` calls each typed Mount<Svc> explicitly. The cli
//     layer hands one of these METHOD VALUES to serve() — selection is
//     compile-time typed, never a string lookup over the inventory.
//
//   - DATA-ONLY INVENTORY — introspection ONLY. `Inventory` is a pure
//     descriptor (names, connect-paths, kind, version) that `forge map` /
//     `forge audit` and the `services` listing read. It carries NO mount
//     closure and is NEVER on the run path. Names here are for DISPLAY.
//
// Construction lives entirely in Build (inject_gen.go). Names are never a
// construction key.
package app

import (
	"log/slog"
	"net/http"

	"connectrpc.com/connect"

	fmw "github.com/reliant-labs/forge/pkg/middleware"
	"github.com/reliant-labs/forge/pkg/mountkit/inventory"
	"{{.Module}}/pkg/config"
	"{{.Module}}/pkg/middleware"
{{- range .Services}}
	{{.Alias}} "{{$.Module}}/{{.ImportPath}}"
{{- end}}
{{- range .ConnectImports}}
	"{{.}}"
{{- end}}
)

// keep imports honest across project shapes.
var (
	_ = connect.WithInterceptors
	_ = middleware.ClaimsFromContext
	_ = fmw.HTTPStack
)

// MountFunc is the TYPED mount signature every per-service Mount<Svc> and
// MountAll satisfy as a METHOD EXPRESSION ((*Services).Mount<Svc>). The cli
// layer's serve() takes one of these directly — selection is compile-time
// typed, never a string→inventory lookup on the run path. Returns the
// Connect paths mounted (for the REST transcoder).
type MountFunc func(s *Services, mux *http.ServeMux, cfg *config.Config, logger *slog.Logger, opts ...connect.HandlerOption) []string

{{range .Services}}
// Mount{{.FieldName}} registers the {{.Name}} service's Connect + HTTP routes
// on mux, reading the constructed instance off s. Returns its Connect path
// in a one-element slice so the method expression (*Services).Mount{{.FieldName}}
// satisfies MountFunc and serve can take it DIRECTLY — no string lookup.
func (s *Services) Mount{{.FieldName}}(mux *http.ServeMux, cfg *config.Config, logger *slog.Logger, opts ...connect.HandlerOption) []string {
{{- if .HasAuthorizer}}
	var authz middleware.Authorizer = {{.Alias}}.NewAuthorizer()
	if cfg.DevAuthBypass() {
		authz = middleware.DevAuthorizer{}
	}
	svcOpts := append(append([]connect.HandlerOption{}, opts...), connect.WithInterceptors(
		middleware.AuthzInterceptor(authz),
	))
	s.{{.FieldName}}.Register(mux, svcOpts...)
{{- else}}
	s.{{.FieldName}}.Register(mux, opts...)
{{- end}}
	s.{{.FieldName}}.RegisterHTTP(mux, fmw.HTTPStack(logger, middleware.ClaimsFromContext))
{{- if .HasWebhooks}}
	s.{{.FieldName}}.RegisterWebhookRoutes(mux, fmw.HTTPStack(logger, middleware.ClaimsFromContext))
{{- end}}
	return []string{ {{.ConnectPkg}}.{{.ProtoServiceName}}Name }
}
{{end -}}

// MountAll mounts every service on mux via the typed per-service Mount
// methods — explicit, compile-time calls, NOT a loop over the data-only
// Inventory. Returns the Connect paths of all mounted services (for the
// REST transcoder). This is the run path for the `server` command. The
// method expression (*Services).MountAll satisfies MountFunc.
func (s *Services) MountAll(mux *http.ServeMux, cfg *config.Config, logger *slog.Logger, opts ...connect.HandlerOption) []string {
{{- if .Services}}
	var mounted []string
{{- range .Services}}
	mounted = append(mounted, s.Mount{{.FieldName}}(mux, cfg, logger, opts...)...)
{{- end}}
	return mounted
{{- else}}
	_, _, _, _ = mux, cfg, logger, opts
	return nil
{{- end}}
}

// MountByName maps a service's runtime name to its TYPED mount func (the
// method expression (*Services).Mount<Svc>), used ONLY by the optional
// `server [names...]` convenience selector. Each entry is a TYPED FUNCTION
// VALUE, not a string lookup over data. Prefer the dedicated
// `<bin> <service>` subcommands; this exists for `server a b c`.
var MountByName = map[string]MountFunc{
{{- range .Services}}
	"{{.Name}}": (*Services).Mount{{.FieldName}},
{{- end}}
}

// Inventory is the project's mountable service inventory. DATA-ONLY:
// introspection (`forge map` / `audit` / `services` listing) reads it; the
// run path never does. Mounting is the typed Mount<Svc> methods above. The
// descriptor TYPE (inventory.ComponentInfo) and its reader helpers
// (inventory.FindByName / inventory.GroupByBaseService) live in
// forge/pkg/mountkit/inventory; this file emits only the data rows.
var Inventory = []inventory.ComponentInfo{
{{- range .Services}}
	{
		Name:        "{{.Name}}",
		ConnectPath: {{.ConnectPkg}}.{{.ProtoServiceName}}Name,
		BaseService: "{{.BaseService}}",
		Version:     "{{.Version}}",
		Kind:        "service",
	},
{{- end}}
}
