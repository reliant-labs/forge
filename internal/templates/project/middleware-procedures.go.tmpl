// Code generated by forge. DO NOT EDIT.
// forge-owned: regenerated every run — do not edit (forge project disown to take ownership)
// Source: proto/services/**/*.proto — every rpc whose (forge.v1.method) sets auth_required: false.

package middleware

{{if .Imports}}import (
{{- range .Imports}}
	{{.Alias}} "{{.Path}}"
{{- end}}
)

{{end}}// UnauthenticatedProcedures is the set of RPCs a caller may reach with no
// credentials. The auth interceptor reads it, and everything not in it is
// rejected before any handler runs.
//
// It is DERIVED, not maintained: an RPC is here because its proto says
//
//	rpc GetStatus(GetStatusRequest) returns (GetStatusResponse) {
//	  option (forge.v1.method) = { auth_required: false };
//	}
//
// and for no other reason. Making an RPC public is one edit, in the file that
// declares the RPC, next to the RPC — and `forge project graph` and this
// set then say the same thing because they read the same annotation.
//
// This file used to be a hand-written map in pkg/middleware, which made two
// declaration surfaces for one fact: a proto could say auth_required: false
// while the map disagreed, and only the map did anything. The proto is the
// declaration; this is its projection.
//
// Entries are connect's own generated procedure constants, so an RPC that is
// renamed or deleted breaks the build here instead of leaving a string that
// matches nothing and silently gates an endpoint that no longer exists.
var UnauthenticatedProcedures = map[string]struct{}{
	// The gRPC health service, which this project does not declare in its
	// own protos: probes run before anything can authenticate.
	"/grpc.health.v1.Health/Check": {},
	"/grpc.health.v1.Health/Watch": {},
{{- range .Open}}
	{{.Const}}: {}, // {{.Service}}/{{.Method}} — auth_required: false
{{- end}}
}
