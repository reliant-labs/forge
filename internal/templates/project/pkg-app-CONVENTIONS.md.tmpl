# pkg/app conventions

`pkg/app` is now a thin user-owned substrate. The LIVE runtime
dependency-injection composition lives in **`internal/app`**
(FORGE_SHAPE_REDESIGN §2): the generated `cmd/server.go` runs

```
OpenInfra (internal/app/providers.go — yours)
  → Build      (internal/app/inject_gen.go — generated, by-type DI)
  → PostBuild  (internal/app/post_build.go — yours, two-phase setters)
  → mount via  internal/app/inventory_gen.go (data-only mount table)
  → serverkit.Run
```

The old name-matched `pkg/app` DI unit (`bootstrap.go`, `wire_gen.go`,
`services_gen.go`, `services.go`, and the `appkit.Def`/`ServiceDef`/`Run`
table) has been **retired**. Construction is typed and owned, resolved by
interface, not by string-keyed name match.

## What's left under pkg/app

- `app_gen.go` (generated) — a minimal `*App` carrier (`DB` / `ORM` +
  the embedded `*AppExtras`). It exists so the user-owned `setup.go`
  compiles.
- `app_extras.go` (yours) — the `AppExtras` extension surface.
- `setup.go` (yours) — infrastructure construction. NOTE: in the §2
  model, infra construction belongs in `internal/app/providers.go`
  (`OpenInfra`); the `setup.go` ↔ `providers.go` reconciliation is a
  known follow-up.
- `post_bootstrap.go` (yours) — post-construction hook reference.
- `testing.go` (generated) — the per-component test harness (below).
- `migrate.go` (generated) — embedded-migration support.

## Where do per-RPC nil checks belong? (Spoiler: nowhere.)

Forge generates `New(Deps) (Service, error)` and a `validateDeps()`
helper. Add your required fields to that single method:

```go
func (d Deps) validateDeps() error {
    if d.Logger == nil   { return fmt.Errorf("foo: Deps.Logger required") }
    if d.Config == nil   { return fmt.Errorf("foo: Deps.Config required") }
    if d.Repo == nil     { return fmt.Errorf("foo: Deps.Repo required") }
    return nil
}
```

Once `Repo` is in `validateDeps`, individual RPC handlers can use
`s.deps.Repo.Get(...)` directly — no `if s.deps.Repo == nil` preamble.
The validation runs once at construction; if it passes, the field is
non-nil for every RPC the service ever handles.

If a Dep really is optional (e.g. an audit sink allowed to be nil in
dev), keep it out of `validateDeps` and check at the one call site that
needs it (mark it `// forge:optional-dep` on the Deps field).

## Component config blocks (configuration ≠ collaborators)

Scalar Deps fields (`MaxPerTick int`, `Interval time.Duration`, ...) are
configuration, not collaborators. Declare a config block in
`proto/config/v1/config.proto` and take it as one typed field
(`Cfg config.<Block>Config`) instead of threading naked scalars through
`Deps`. `forge lint --config-deps` flags naked-scalar Deps fields.

## Test wiring

`pkg/app/testing.go` emits per-component `NewTest<Svc>(t, ...opts)`
factories. Tests construct `Deps` directly (filling in only the fields
they care about; defaults flow in for the bare-Deps trio) and call
`New` — the production DI graph is bypassed in tests so each test stays
self-contained. Use `app.With<Svc>Deps(deps)` to override the rich deps.
See the `testing/patterns` skill for the canonical patterns.
