package {{.Package}}

import "context"

// Authorizer handles access control for the {{.ServiceName}} service.
// The default implementation allows requests through unchanged.
// Add service-specific authorization checks here when needed.
type Authorizer struct{}

// NewAuthorizer creates a new Authorizer for this service.
func NewAuthorizer() *Authorizer {
	return &Authorizer{}
}

// CanAccess implements the Authorizer interface.
func (a *Authorizer) CanAccess(_ context.Context, _ string) error {
	return nil
}
