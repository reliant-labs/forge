package {{.ServiceName | lower}}

{{- if .Methods}}

import (
	"context"
	"fmt"

	"connectrpc.com/connect"

	pb "{{.Module}}/gen/{{.ProtoPackage}}/v1"
)

{{range .Methods}}
{{- if and .ClientStreaming .ServerStreaming}}
// {{.Name}} implements the {{.Name}} bidirectional streaming RPC.
// TODO: Implement business logic for {{.Name}}.
func (s *Service) {{.Name}}(
	ctx context.Context,
	stream *connect.BidiStream[pb.{{.InputType}}, pb.{{.OutputType}}],
) error {
	return connect.NewError(connect.CodeUnimplemented, fmt.Errorf("{{.Name}} not yet implemented"))
}
{{- else if .ClientStreaming}}
// {{.Name}} implements the {{.Name}} client streaming RPC.
// TODO: Implement business logic for {{.Name}}.
func (s *Service) {{.Name}}(
	ctx context.Context,
	stream *connect.ClientStream[pb.{{.InputType}}],
) (*connect.Response[pb.{{.OutputType}}], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, fmt.Errorf("{{.Name}} not yet implemented"))
}
{{- else if .ServerStreaming}}
// {{.Name}} implements the {{.Name}} server streaming RPC.
// TODO: Implement business logic for {{.Name}}.
func (s *Service) {{.Name}}(
	ctx context.Context,
	req *connect.Request[pb.{{.InputType}}],
	stream *connect.ServerStream[pb.{{.OutputType}}],
) error {
	return connect.NewError(connect.CodeUnimplemented, fmt.Errorf("{{.Name}} not yet implemented"))
}
{{- else}}
// {{.Name}} implements the {{.Name}} RPC.
// TODO: Implement business logic for {{.Name}}.
func (s *Service) {{.Name}}(
	ctx context.Context,
	req *connect.Request[pb.{{.InputType}}],
) (*connect.Response[pb.{{.OutputType}}], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, fmt.Errorf("{{.Name}} not yet implemented"))
}
{{- end}}
{{end}}
{{- else}}

// Handler implementations will be generated by 'forge generate'.
// Run 'forge generate' after defining your RPCs in the proto file.
{{- end}}
